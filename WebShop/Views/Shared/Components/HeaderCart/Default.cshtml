@model List<WebShop.ModelViews.CartItem>
@if (Model != null && Model.Count() > 0)
{
    @*<div class="harmic-offcanvas-body">
        <div class="minicart-content">
            <div class="minicart-heading">
                <h4 class="mb-0">Giỏ hàng</h4>
                <a href="#" class="button-close"><i class="pe-7s-close"></i></a>
            </div>
            <ul class="minicart-list">
                @foreach (var item in Model)
                {
                    string url = $"/{item.product.Alias}-{item.product.ProductId}.html";
                    <li class="minicart-product">
                        <a href="@url" class="product-item_img">
                            <img class="img-full" src="~/images/products/@item.product.Thumb" alt="@item.product.ProductName">
                        </a>
                        <div class="product-item_content">
                            <a class="product-item_title" href="@url">@item.product.ProductName</a>
                            <span class="product-item_quantity">@item.amount x @item.product.Price.Value.ToString("#,##0") VNĐ</span>
                        </div>
                    </li>
                }
            </ul>
        </div>
        <div class="minicart-item_total">
            <span>Tổng tiền</span>
            <span class="ammount">@Model.Sum(x => x.TotalMoney).ToString("#,##0") VNĐ</span>
        </div>
        <div class="group-btn_wrap d-grid gap-2">
            <a href="cart.html" class="btn btn-secondary btn-primary-hover">Xem giỏ hàng</a>
            @if (User.Identity.IsAuthenticated)
            {
                <a href="/checkout.html" class="btn btn-secondary btn-primary-hover">Thanh toán</a>
            }
            else
            {
                <a href="/dang-nhap.html?returnUrl=/checkout.html" class="btn btn-secondary btn-primary-hover">Thanh toán</a>
            }
        </div>
    </div>*@

            <!-- Start  Offcanvas Addcart Wrapper -->
            <div class="offcanvas-add-cart-wrapper">
                <h4 class="offcanvas-title">Giỏ Hàng</h4>
                <ul class="offcanvas-cart">
            @foreach (var item in Model)
            {
                string url = $"/{item.product.Alias}-{item.product.ProductId}.html";
                         <li class="offcanvas-cart-item-single">
                         <a href="@url"class="offcanvas-cart-item-image-link">
                                    <img  src="~/images/products/@item.product.Thumb" alt="@item.product.ProductName" class="offcanvas-cart-image">
                                </a>
                                 <div class="offcanvas-cart-item-content">
                                    <a class="offcanvas-cart-item-link" href="@url">@item.product.ProductName</a>
                                     <div class="offcanvas-cart-item-details">
                                        <span class="offcanvas-cart-item-details-quantity">@item.amount x </span>
                                        <span class="offcanvas-cart-item-details-price">@item.product.Price.Value.ToString("#,##0") VNĐ</span>
                                    </div>

                                </div>
                         </li>
            }
                </ul>

                <div class="offcanvas-cart-total-price">
                    <span class="offcanvas-cart-total-price-text">Tổng tiền:</span>
                    <span class="offcanvas-cart-total-price-value">@Model.Sum(x => x.TotalMoney).ToString("#,##0") VNĐ</span>
                </div>
                <ul class="offcanvas-cart-action-button">
                        <li><a href="cart.html" class="btn btn-block btn-golden">Xem giỏ hàng</a></li>

            @if (User.Identity.IsAuthenticated)
            {
                        <li><a href="/checkout.html" class=" btn btn-block btn-golden mt-5">Thanh toán</a></li>
            }
            else
            {
                         <li><a href="/dang-nhap.html?returnUrl=/checkout.html" class=" btn btn-block btn-golden mt-5">Thanh toán</a></li>
            }
                </ul>
            </div> <!-- End  Offcanvas Addcart Wrapper -->
}
else
{
  @*<div class="harmic-offcanvas-body">
        <div class="minicart-content">
            <div class="minicart-heading">
                <h4 class="mb-0">Giỏ hàng rỗng</h4>
            </div>
        </div>
   </div>
*@

              <div class="offcanvas-add-cart-wrapper">
                <h4 class="offcanvas-title">GIỎ HÀNG RỖNG</h4>
                </div>

}



   
        


    